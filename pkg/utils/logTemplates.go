package utils

const ArgParseError = "Error while parsing CLI args %v\n"

const SleepTemplate = "Sleeping for %d minutes\n"

const ConnectionOK = "Connected to device %s\n"

const ConnectionError = "Could not connect to device because %v\n"

const CommandSend = "Sending %s command\n"

const CommandError = "Error while sending command %s: %v\n"